import { InDateRangeDirective } from './in-date-range.directive';

describe('InDateRangeDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new InDateRangeDirective({});
    expect(directive).toBeTruthy();
  });
});
